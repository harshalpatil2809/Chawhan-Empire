# DEMO ONLY - these defaults are also hardcoded as fallbacks in the demo login route.
# Replace this whole mechanism with a real auth provider before launch.
DEMO_ADMIN_EMAIL=admin@buildcraft.demo
DEMO_ADMIN_PASSWORD=demo123

# When you connect a real database, add its connection string here.
# DATABASE_URL=
